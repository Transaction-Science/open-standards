[package]
name = "joule-ui-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "joule-ui protocol core — typed component specs (Widget / PropValue / WidgetSchema / Registry) and the recursive validator. Pure Rust, no IO, JSON-serialisable. The renderer is the host's."

[dependencies]
serde      = { workspace = true }
serde_json = { workspace = true }
thiserror  = { workspace = true }
