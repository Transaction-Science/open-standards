[package]
name = "op-routing"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "OpenPay advanced routing: least-cost routing (LCR), MCC-aware routing, and intelligent retry / soft-decline recovery. Composable primitives that ingest into op-orchestrator's PolicyRouter."

[dependencies]
op-core = { path = "../op-core" }
thiserror = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }
