# EOC cookbook — pinned dependencies for the runnable example.
# Tested with Python 3.11 and 3.12 on macOS (Apple Silicon) and Linux (x86_64).

transformers==4.46.3
sentence-transformers==3.3.1
datasets==3.1.0
evaluate==0.4.3
torch==2.5.1
numpy==2.1.3
accelerate==1.1.1

# Optional — hardware energy counters. Both packages degrade gracefully
# when absent; the recipe falls back to a calibrated estimator.
# pyRAPL==0.2.3.1     ; sys_platform == "linux"
# pynvml==12.0.0
