[package]
name = "jouleclaw-promote"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "JouleClaw yang→yin promotion — a verified model answer becomes a permanent, provenance-stamped deterministic fact, so the same query never pays the model tax again. Cost trends to zero with use."

[dependencies]
jouleclaw-cascade = { path = "../jouleclaw-cascade" }
blake3            = { workspace = true }
serde            = { workspace = true }
serde_json       = { workspace = true }
thiserror        = { workspace = true }
